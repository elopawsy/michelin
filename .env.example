# Base de données PostgreSQL.
# En local, démarre Postgres avec `docker compose up -d db` puis utilise :
DATABASE_URL="postgres://michelin:michelin@localhost:55432/michelin"

# Secret de signature des JWT (changer en production).
JWT_SECRET="change-this-dev-secret"
